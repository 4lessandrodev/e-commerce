import {
	COMMENT_TEXT_MIN_STRING_LENGTH,
	COMMENT_TEXT_MAX_STRING_LENGTH
} from './comment.domain-entity';
export const ERROR_COMMENT_TEXT_LENGTH = `Coupon Description length Should be between ${COMMENT_TEXT_MIN_STRING_LENGTH} and ${COMMENT_TEXT_MAX_STRING_LENGTH}`;
