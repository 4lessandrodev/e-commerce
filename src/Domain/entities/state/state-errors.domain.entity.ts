import {
	STATE_NAME_MAX_STRING_LENGTH,
	STATE_NAME_MIN_STRING_LENGTH
} from './state.domain-entity';

export const ERROR_STATE_DESCRIPTION_LENGTH = `State Name length Should be between ${STATE_NAME_MIN_STRING_LENGTH} and ${STATE_NAME_MAX_STRING_LENGTH}`;
