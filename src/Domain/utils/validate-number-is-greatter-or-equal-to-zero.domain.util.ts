/**
 *
 * @param value number to validate
 * Returns `true` if value is greatter or equal to zero and `false`if not
 */
export const validateNumberGreatterOrEqualToZero = (value: number): boolean => {
  return value >= 0;
};
