import { MAX_ADDRESS_NUMBER_LENGTH } from './AddressNumber.value-object';
export const ERROR_ADDRESS_NUMBER_LENGTH = `Invalid number description length, must be less than ${MAX_ADDRESS_NUMBER_LENGTH} character`;
