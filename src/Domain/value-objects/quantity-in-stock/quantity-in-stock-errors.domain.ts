import {
  MAX_STOCK_QUANTITY_VALUE,
  MIN_STOCK_QUANTITY_VALUE,
} from './quantity-in-stock.value-object';

export const ERROR_INVALID_STOCK_VALUE = `Stock value must be between ${MIN_STOCK_QUANTITY_VALUE} and ${MAX_STOCK_QUANTITY_VALUE}`;
