import {
  MAX_EXCHANGE_FACTOR,
  MIN_EXCHANGE_FACTOR,
} from './BasketItem.value-object';

export const ERROR_INVALID_EXCHANGE_FACTOR_RANGE = `Invalid range for Exchange Factor, must be between ${MIN_EXCHANGE_FACTOR} and ${MAX_EXCHANGE_FACTOR} `;
