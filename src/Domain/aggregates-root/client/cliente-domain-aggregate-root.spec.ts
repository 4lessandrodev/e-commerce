describe('cliente-domain-aggregate-root', () => {
  it('should be defined', () => {
    /*   const client = Client.create({
      address: AddressValueObject.create().getResult(),
      subscriptions: ['valid_order_id'],
    });
    expect(client.isSuccess).toBe(true); */
  });
});
